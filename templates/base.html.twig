<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Recruitment-task{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        {% endblock %}
        {% block javascripts %}
        {% endblock %}
    </head>
    <body>
        <header>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <span class="navbar-brand mb-0 h1">Cogitech-recruitment-tast-solution</span>
                <div class="navbar-nav">
                    {%  if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="nav-item nav-link" href="{{ path("app_list") }}">Posts</a>
                        <a class="nav-item nav-link" href="{{ path("app_logout") }}">Logout</a>
                    {% else %}
                        <a class="nav-item nav-link" href="{{ path("app_posts") }}">Posts</a>
                        <a class="nav-item nav-link" href="{{ path("app_login") }}">Login</a>
                        <a class="nav-item nav-link" href="{{ path("app_register") }}">Register</a>
                    {% endif %}
                </div>
            </nav>
        </header>
        <main class="mt-5">
            <div class="container d-flex flex-wrap justify-content-between">
                {% block body %}
                {% endblock %}
            </div>
        </main>
        <footer class="footer fixed-bottom py-1 bg-primary d-flex justify-content-center">
            <span>&copy; 2023 Tomasz Sokalski</span>
        </footer>
    </body>
</html>